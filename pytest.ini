[pytest]
log_format = %(asctime)s %(name)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
# Show logs for every test, not only failed ones. A test that hangs prints
# nothing otherwise.
log_cli = True
# log_cli_level = DEBUG
